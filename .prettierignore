node_modules
dist
build
test-results
playwright-report
tests/e2e/.output
pnpm-lock.yaml

# Markdown is hand-authored prose + tables; prettier's line-wrapping and table
# realignment fight the author more than they help. Kept unformatted.
*.md

# Reference art-style mockup (doc/setting.md treats it as visual source of
# truth; regenerated by hand when the art direction moves).
doc/art-style-samples.html
